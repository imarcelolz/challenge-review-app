import React from 'react'
import { HomeProps, connector } from './Home.map'

const HomePage: React.FC<HomeProps> = (props) => {
  return <React.Fragment></React.Fragment>
}

export default connector(HomePage)
